using System;

namespace Overview.Breakdown
{
    class Breakdown
    {
        public Breakdown()
        {
            object s = new Strings(),
                d = new DateTimes(),
                t = new Types(),
                c = new ControlFlow(),
                cs = new Collections();
            Console.Read();
        }
    }
}
